# The former width-validation pass rewrote box-clamp kinematics and cut the
# final BASE to suit the inboard redesign. That redesign is intentionally
# retired. Keep the independent validators aligned with the restored proven
# clamp mechanism; transverse width will be addressed as a separate compacting
# step after mechanics are green again.
print('Width validation: inboard clamp rewrite disabled')
